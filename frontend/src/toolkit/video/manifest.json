{
  "manifest": "smoothie.processor.v1",
  "name": "video",
  "version": "0.2.0",
  "commands": [
    {
      "name": "extract_frames",
      "description": "Extract frames from a video - a single frame at a timestamp, or a set over a range.",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/extract_frames.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    },
    {
      "name": "keyframes",
      "description": "Extract representative keyframes (I-frames) from a video - one image per visual",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/keyframes.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    },
    {
      "name": "probe",
      "description": "Probe a video's container/streams with ffprobe -> duration, fps, resolution, codecs.",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/probe.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    },
    {
      "name": "scene_detect",
      "description": "Detect scene/shot boundaries (content-aware) -> timestamp ranges.",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/scene_detect.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    },
    {
      "name": "sentiment_segments",
      "description": "Group a transcript's segments into consecutive runs of the same sentiment, with",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/sentiment_segments.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    },
    {
      "name": "transcribe",
      "description": "Transcribe a video/audio file to timestamped segments with faster-whisper.",
      "run": "uv run \"$SMOOTHIE_TOOLKIT/video/transcribe.py\" \"$SMOOTHIE_SOURCE_PATH\" --json",
      "emits": "text"
    }
  ]
}
